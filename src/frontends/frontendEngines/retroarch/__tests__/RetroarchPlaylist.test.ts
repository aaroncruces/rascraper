/*
jest.mock("../../../io/apiRequest");
jest.setTimeout(500000); //if testing screenscraper online (goes really slow sometimes)
*/
describe("", () => {
  it("creates a RetroarchPlaylistGameItemJSONObject from a game item", () => {
    expect(true).toBe(true);
  });
});
//
